<Window x:Class="PL.Components.SchedulerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Components"
        mc:Ignorable="d"
        Title="SchedulerWindow" Height="450" Width="800"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}"
        >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Visibility="Visible" Grid.Column="0" Grid.Row="0"  HorizontalAlignment="Center" VerticalAlignment="Center">
            <DatePicker  HorizontalAlignment="Center" VerticalAlignment="Center" SelectedDate="{Binding ProjectStartDate}" />
            <Button Content="Start Production" MaxWidth="100" Click="startProduction_Click"/>
        </StackPanel>
        
    </Grid>

</Window>
